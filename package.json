{
  "name": "ops-ticket-api",
  "version": "1.0.0",
  "private": true,
  "description": "Broken DevOps live coding API for interview candidates",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "lint": "eslint src scripts tests",
    "test": "node --test tests/*.test.js",
    "check:ready": "node scripts/check-ready.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "nodemon": "^3.1.9",
    "supertest": "^7.0.0"
  }
}
